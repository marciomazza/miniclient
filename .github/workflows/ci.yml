name: CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_call:
permissions:
  contents: read
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          # Bust the old cache: it stored v8 as "built" but without librusty_v8.a,
          # so linking failed. Bump this suffix again if it re-poisons.
          key: v2-lint
      - run: uv sync
      - run: uv run prek run --all-files

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Clone vendor/htmx (via HTTPS, no SSH key in CI)
        run: |
          git config --global url."https://github.com/".insteadOf "git@github.com:"
          bash scripts/setup-vendor-htmx.sh
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test
      - run: uv sync
      - run: uv run pytest tests/ -n auto --cov-report=term-missing
      - uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
